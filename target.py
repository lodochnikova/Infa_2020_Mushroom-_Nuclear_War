class target():
    def __init__(self, hp, holder_name):
        self.hp = 100
        self.holder_name = holder_name
        
    def hit(self, striker_name):
        if atriker_name != self.holder_name
            self.hp -= 10

    def refit(self, helper_name):
        if helper_name == self.holder_name
            self.hp += 10
